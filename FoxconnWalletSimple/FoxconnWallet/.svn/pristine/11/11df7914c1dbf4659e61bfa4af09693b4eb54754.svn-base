package com.zeustel.cp.views;

import android.content.Context;
import android.view.View;
import android.widget.LinearLayout;

import com.zeustel.cp.bean.PopView;
import com.zeustel.cp_sdk.R;

/**
 * Created by Do on 2016/2/23.
 */
public class Closeable  extends PopView{
    private View rootView;
//    private ImageView close;
    protected LinearLayout layout;
    public Closeable(Context context){
        super(context);
    }

    @Override
    protected void initView() {
        rootView = inflate(getContext(), R.layout.view_closeable, this);
//        close = (ImageView)findViewById(R.id.closeable_close);
        layout = (LinearLayout)findViewById(R.id.closeable_layout);

//        close.setOnClickListener(this);
    }



    @ Override
    public void onClick(View v) {
        switch (v.getId()){
//            case R.id.closeable_close:
//                close();
//                break;
        }
    }
}
